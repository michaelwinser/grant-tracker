# Staging environment (Cloud Run)
# Copy to staging.env and fill in your values
# Deploy with: ./gt gcp deploy staging

GCP_PROJECT_ID="your-project-id"
GCP_REGION="us-central1"

# OAuth credentials (from GCP Console -> APIs & Services -> Credentials)
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# Staging redirect URI - must be registered in OAuth client
REDIRECT_URI="https://your-staging-domain.example.com/auth/callback"

# Separate Cloud Run service for staging
SERVICE_NAME="grant-tracker-staging"
