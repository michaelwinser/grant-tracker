# Production environment (Cloud Run)
# Copy to prod.env and fill in your values
# Deploy with: ./gt gcp deploy prod

GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1

# OAuth credentials (from GCP Console -> APIs & Services -> Credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Production redirect URI - must be registered in OAuth client
REDIRECT_URI=https://your-domain.example.com/auth/callback

# Production Cloud Run service
SERVICE_NAME=grant-tracker

# Service account credentials (for backend API access)
# Save the JSON key file to: .secrets/service-account-key.json
# Get this from GCP Console -> IAM -> Service Accounts -> Keys -> Add Key -> JSON
# The file is automatically uploaded to Secret Manager during deploy

# Data source (required when using service account)
# SPREADSHEET_ID=your-spreadsheet-id
# GRANTS_FOLDER_ID=your-grants-folder-id
